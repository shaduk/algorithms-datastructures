#include <iostream>
#include <queue>
using namespace std;

int main()
{
   	int sval, lval, N;
   	queue<int> myq;
   	cin >> sval >> lval;
   	cin >> N;
   	for(int i = 0; i < N; i++)
   	{
   		int input;
   		cin >> input;
   		myq.push(input);
   	}
   	
   	while(sval)
}
